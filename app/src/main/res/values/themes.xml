<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CashMap" parent="Theme.Material3.DynamicColors.Light.NoActionBar">
        <item name="android:actionBarStyle">@style/ActionBar</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/cashmap_main</item>
    </style>

    <style name="Theme.CashMap"  parent="Base.Theme.CashMap" />
    <style name="ActionBar" parent="android:Widget.ActionBar">
        <item name="backgroundColor">@color/cashmap_main</item>
        <item name="backgroundTint">@color/cashmap_main</item>
        <item name="background">@color/cashmap_main</item>
    </style>
</resources>